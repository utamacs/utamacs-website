node_modules/
dist/
.astro/
.DS_Store
*.log
.claude/settings.local.json

# ─── Environment / Secrets — NEVER commit real credentials ──────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.cert
*.crt
*_rsa
*_dsa
*_ecdsa
*_ed25519
.npmrc
.netrc
secrets/
credentials/
**/credentials.json
**/service-account*.json
**/serviceAccount*.json

# ─── Playwright ─────────────────────────────────────────────────────────────
# tests/.auth contains Supabase session tokens — NEVER commit
tests/.auth/
playwright-report/
test-results/

# ─── Upload safety net ──────────────────────────────────────────────────────
# All user-uploaded files MUST go to Supabase Storage via SupabaseStorageService.
# The HOTO governance upload is the only exception and it goes to a SEPARATE
# governance GitHub repo via GitHub API — never to this repo's working tree.
# These patterns prevent any accidental filesystem write from reaching git.
uploads/
tmp/
temp/
public/uploads/
src/uploads/
docs/uploads/
